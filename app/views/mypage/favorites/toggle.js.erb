$_favorite = $('.favorite_star[data-pid=<%= @product_id %>]')
<% if @favorite.soft_destroyed_at.present? %>
  $_favorite.removeClass("active").attr("data-original-title", "お気に入りに登録")
<% else %>
  $_favorite.addClass("active").attr("data-original-title", "お気に入りから削除")
<% end %>
