- set_meta_tags title: "入札"
- breadcrumb  :system_bids_new

= render "/system/main/select_company", { companies_selector: @companies_selector, company_id: session[:system_company_id] }

= render "/bid/bids/new", { open_now: @open_now, product: @product, bid: @bid, bids: @bids, search: @search } if @company
