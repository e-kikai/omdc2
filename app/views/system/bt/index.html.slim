- set_meta_tags title: "BTメニュー"

ul.nav
  li = link_to "入庫処理", "/system/bt/new", class: "#{:disabled unless @open_now.entry_start?}"
  li = link_to "出庫処理", "/system/bt/carryout", class: "#{:disabled unless @open_now.result_sum?}"
